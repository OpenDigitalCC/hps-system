# Minimal test kickstart
text
lang en_GB.UTF-8
keyboard uk
timezone --utc UTC
network --bootproto=dhcp --device=link --onboot=yes
rootpw --plaintext changeme
zerombr
clearpart --all --initlabel
autopart
reboot

%packages
@core
%end


